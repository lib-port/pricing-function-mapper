# Model card template

Every exported artifact contains a rendered `model-card.md` with intended use,
selected estimator family, held-out audit metrics, conformal coverage, warnings,
and explicit governance limitations. The implementation renders the template
from versioned metadata rather than serializing Python validation objects.

